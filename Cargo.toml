[package]
name = "settings_as_constants"
version = "0.1.0"
edition = "2024"

[dependencies]

[build-dependencies]
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

[profile.dev]
opt-level = 3
